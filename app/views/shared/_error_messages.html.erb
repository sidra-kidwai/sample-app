<% if @post.errors.any? %>
  <div>
    <p><%= pluralize(@post.errors.count, "error") %> prevented it from being saved.</p>
    <ul>
      <% @post.errors.full_messages.each do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  </div>
<% end %>

